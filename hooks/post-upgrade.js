#!/usr/bin/env node

/**
 * Post-upgrade hook for zylos-coco-workspace.
 * Handles config.json field migration if schema changes between versions.
 */

// TODO: implement config migration logic when schema evolves

console.log('[zylos-coco-workspace] post-upgrade complete (no migrations needed)');
