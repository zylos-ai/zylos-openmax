#!/usr/bin/env node

/**
 * ArtifactStore CLI.
 * Wraps AS service HTTP API.
 *
 * Usage: node as.js <command> '<json>'
 * Example: node as.js as.upload '{"filePath":"/tmp/report.pdf"}'
 *
 * Provided by: AS team
 */

// TODO: implement as.upload, as.get, as.url, as.list
